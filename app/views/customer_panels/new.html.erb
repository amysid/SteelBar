<section class="custom-wrap">
  <div class="container">
    <div class="Dashboard-section">
      <h4>Create Customer Panel</h4>
      <%= form_with model: @cp, id: "customer-panel" do |f| %>
        <%= render "form", f: f %>
      <% end %>
    </div>
  </div>
</section>