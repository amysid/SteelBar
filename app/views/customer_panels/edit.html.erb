<section class="custom-wrap">
  <div class="container">
    <div class="Dashboard-section">
      <h4>Edit Customer Panel</h4>
      <%= form_with scope: @cp, url: customer_panel_path, method: :put, id: "new_cp", local: true do |f|%>
        <%= render "form", f: f %>
      <% end %>
    </div>
  </div>
</section>