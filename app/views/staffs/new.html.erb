<section class="custom-wrap">
  <div class="container">
    <div class="Dashboard-section">
      <h4>Create Staff</h4>
      <%#= form_with scope: @staff, url: staff_path, id: "new_staff", local: true do |f|%>
      <%= form_with model: @staff do |f| %>
        <%= render "form", f: f %>
      <% end %>
    </div>
  </div>
</section>