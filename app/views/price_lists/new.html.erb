<section class="custom-wrap">
  <div class="container">
    <div class="Dashboard-section">
      <h4>Add Details</h4>
      <%= form_with model: @pl, id: "price-list" do |f| %>
        <%= render "form", f: f %>
      <% end %>
    </div>
  </div>
</section>